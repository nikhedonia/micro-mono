{
  "name": "micro-mono",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "private": "true",
  "author": "Gaetano Checinski",
  "description": "minimal microfrontend mono repo template. I went through all the pitfalls so you don't have to",
  "scripts": {
    "build": "wsrun build",
    "dev": "wsrun webpack-dev-server",
    "start": "wsrun start",
    "clear": "rm -rf packages/*/out"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^6.2.1",
    "serve": "^12.0.1",
    "wsrun": "^5.2.4"
  }
}
